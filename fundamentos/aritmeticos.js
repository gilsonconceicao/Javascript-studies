// operadores aritméticos 

// soma, subtração

const [a, b, c, d] = [7, 10, 12, 8]; 

const soma = a + b + c + d; 
const subtracao = b - d; 
const multiplicao = a * b;
const divisao = d / a; 
const modulo = c % a 

console.log(soma, subtracao, multiplicao, divisao, modulo); 