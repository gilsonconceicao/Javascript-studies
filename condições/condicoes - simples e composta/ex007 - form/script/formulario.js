// dados do formulário

// e-mail

function click_submit() {
    var email = window.document.getElementById('email_input'); 
    var senha = window.document.getElementById('senha'); 
    email = email_input.value; 
    console.log (
        email
    )
    console.log (
        senha
    )
}

// apenas um teste mesmo. 